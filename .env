# Example .env.example — copy to .env.local and fill with your credentials
#
# Gmail (Google Workspace / G-Suite) recommended: generate an App Password for your account
# and set SMTP_USER to your email (Info@pro-option.sa). Use the generated app password
# as SMTP_PASS. If you have OAuth2 set up, you can replace the SMTP auth with an OAuth2
# token and modify the transport accordingly.

# SMTP server host (Gmail)
SMTP_HOST=smtp.gmail.com

# SMTP port: 465 for SSL, 587 for STARTTLS
SMTP_PORT=587

# Your Gmail / Google Workspace email — this will be used as the 'from' address
SMTP_USER=Info@pro-option.sa

# For Gmail with 2FA: create an App Password and use it here
# Read: https://support.google.com/accounts/answer/185833
SMTP_PASS=Po@@54321

# Where to forward contact submissions — defaults to SMTP_USER if unset
CONTACT_TO=Info@pro-option.sa

# Optional: Disable maintenance to show the public site
NEXT_PUBLIC_MAINTENANCE_MODE=false
